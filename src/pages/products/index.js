import { Categories }  from'./js/cetegory.options'
import { SwiperFicha }  from'./js/ficha.swiper'

const page = document.querySelector('.page__products')

if (page) {
    Categories()
    SwiperFicha()
}